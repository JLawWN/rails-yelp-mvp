<html>
  <header>
  </header>
  <body>
    <div class="review-form">
      <%= simple_form_for [@restaurant, @review] do |f| %>
      <%= f.input :content, as: :text%>
      <%= f.input :rating, as: :integer, placeholder: 'rate between 0-5' %>
      <%= f.submit "add a review", class: "btn btn-primary" %>
      <% end %>
    </div>
  </body>
</html>
